stages:
  - move
  - build

build:
  stage: build
  script:
    - echo "Building the application on SSH server..."
    - hostname
    - pwd
    - ssh -p 22 david@172.26.0.41  "cd /home/david/application/wallet/ && chmod +x ./gradlew && ./gradlew clean && ./gradlew build -x test && systemctl restart wallet"
    - echo "Source code built on remote server"
  only:
    - main

move:
  stage: move
  script:
    - echo "Copying artifacts from remote server to GitLab CI/CD..."
    - hostname
    - pwd
    - scp   -P 22 -r ./*  david@172.26.0.41:/home/david/application/wallet/ 
    - echo "Artifacts uploaded to GitLab CI/CD."
  only:
    - main
